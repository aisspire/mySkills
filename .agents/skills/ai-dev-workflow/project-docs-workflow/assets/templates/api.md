# API 摘要：<模块名>

> 面向 AI 和开发者的快速接口地图。完整前端联调文档仅在明确要求时生成，并在此处链接。

## 范围与关联流程

- 功能：
- 流程：
- 详细前端文档：

## 接口地图

| 接口 | 方法 | 路径 | 用途 | 请求对象/关键入参 | 返回类型/关键字段 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 关键契约

只记录无法从接口名称和对象类型直接看出的业务语义，例如：

- 列表与详情返回差异。
- 字段从上游接口传给下游接口的规则。
- 分页、排序、幂等、权限或状态约束。
- 兼容路径或废弃字段。

## 接口与流程关系

| 业务动作 | 接口 | 依赖输入 | 关键输出 | 下一步 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 已知不一致与兼容性

只记录当前仍存在的问题，不保留已解决历史。
